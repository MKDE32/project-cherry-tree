VPN einrichten

sudo openvpn user.ovpn

the user.ovpn file is the VPN key that we download from either the Academy module section or the main HTB Access page
If we type ifconfig in another terminal window, we will see a tun adapter if we successfully connected to the VPN.
