# which
```
which tcpdump
```
>/usr/sbin/tcpdump

# install
```
sudo apt update
sudo apt full-upgrade
sudo apt install tcpdump
```
# version
```
sudo tcpdump --version
```
>tcpdump version 4.9.4

























