# INSTALL
```
sudo apt update
sudo apt install git
git --version
```

# (INITIAL CONFIG)
```
git config --global user.name "Dein Name"
git config --global user.email "mail@example.com"
```

# IF APT CANT FIND GIT
```
sudo apt install git-all
```




