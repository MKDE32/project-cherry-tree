sudo rechte verwalten
es empfiehlt sich stark diese datei nicht manuell sondern mit dem befehl visudo zubearbeiten

User privilege specification
 root   ALL=(ALL:ALL) ALL
is the users allowed to use the sudo command. 
defines the hosts (servers) on which sudo can be employed.
is the user you are running the command as.
defines the commands allowed.

allow a user specific command
user ALL=(ALL) 	NOPASSWD: /sbin/fdisk
